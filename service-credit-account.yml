server:  port: 8074spring:  data:    mongodb:      database: CreditAccount      host: localhost      port: 27017  application:    name: SERVICE-CUSTOMER
eureka:
  client:
    service-url:
      register-with-eureka: true
      fetch-registry: true
      defaultZone: http://localhost:8761/eureka
  instance:
     hostname: localhost
      
#